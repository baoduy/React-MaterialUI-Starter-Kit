const NotificationItemStyle = theme => ({
  icon: {
    fontSize: 20
  },

  message: {
    display: "flex",
    alignItems: "center",
    whiteSpace: "pre"
  }
});

export default NotificationItemStyle;
